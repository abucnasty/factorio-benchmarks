#!/bin/bash
# Explicit pin to all 16 hardware threads (mirrors default behavior)
FACTORIO_BIN="${HOME}/Games/factorio_beta/bin/x64/factorio"
exec taskset -c 0-15 gamemoderun "${FACTORIO_BIN}" "$@" --cache-sprite-atlas=true
